services:
  pb:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./pb_migrations:/pb/pb_migrations
      - pb_data_volume:/pb/pb_data
    ports:
      - "8080:8080"

volumes:
  pb_data_volume:
