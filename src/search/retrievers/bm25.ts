// import { qdrantClient } from "@/shared/lib/qdrant";

// class BM25Retriever {
//   constructor() {}

//   async createRetriever(orgId: string) {
//     const collectionName = `org_${orgId}`;

//     const retriever = await BM25Retriever.fromExistingCollection(
//       this.embeddings,
//       { url: QDRANT_URL, collectionName, apiKey: QDRANT_API_KEY }
//     );
//   }
// }
