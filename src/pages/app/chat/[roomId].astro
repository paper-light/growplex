---
import AppDashboard from "../../../shared/ui/layouts/AppDashboard.astro";
import ChatLayout from "../../../chat/ui/ChatLayout.svelte";

const { roomId } = Astro.params;
---

<AppDashboard title="Chat Rooms">
  <ChatLayout roomId={roomId} client:only transition:persist="chat-layout" />
</AppDashboard>
