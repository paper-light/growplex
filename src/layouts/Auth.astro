---
import AppGlobalState from "../app/AppGlobal.svelte";
---

<AppGlobalState client:only transition:persist name="app" />

<main class="mx-auto mt-8 max-w-2xl">
  <div class="bg-card border-card rounded-lg border p-4 shadow-lg">
    <slot />
  </div>
</main>
