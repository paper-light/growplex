---
import AppDashboard from "@/pages/_layouts/app/AppDashboard.astro";

import Sidebar from "@/integration/ui/Sidebar.svelte";
import ChatPreview from "@/chat/ui/widgets/ChatPreview.svelte";

import Wizard from "./_index.svelte";
---

<AppDashboard title="Integration Wizard">
  <div class="w-full h-full relative">
    <!-- Desktop Layout: Grid with flexible main content -->
    <div
      class="hidden lg:grid h-full w-full"
      style="grid-template-columns: minmax(200px, 320px) minmax(400px, 1fr) minmax(340px, 400px);"
    >
      <div class="h-full overflow-hidden">
        <Sidebar block client:load />
      </div>

      <div class="h-full overflow-hidden">
        <div class="h-full flex flex-col p-4 gap-2 overflow-y-auto">
          <Wizard client:load />
        </div>
      </div>

      <div class="h-full overflow-hidden">
        <ChatPreview block client:load />
      </div>
    </div>

    <!-- Mobile Layout: Single column with overlays -->
    <div class="lg:hidden w-full h-full relative">
      <div class="h-full flex flex-col p-4 gap-2 overflow-y-auto">
        <Wizard client:load />
      </div>
      <Sidebar client:load />
      <ChatPreview client:load />
    </div>
  </div>
</AppDashboard>
