---
import "../../styles/global.css";
import { ClientRouter } from "astro:transitions";

import Posthog from "../components/Posthog.astro";
import ThemeLoad from "../components/ThemeLoad.astro";
import CookiesAccept from "../components/CookiesAccept.svelte";
import PortalHost from "../../ui/PortalHost.svelte";

import { getLocale } from "../../paraglide/runtime";

const { title, favicon = "/LOGO.png" } = Astro.props;
---

<!doctype html>
<html lang={getLocale()}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />

    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="icon" type="image/svg+xml" href={favicon} />

    <title>{title}</title>

    <ThemeLoad />
    <ClientRouter />
    <Posthog />

    <slot name="head" />
  </head>
  <body>
    <slot />

    <CookiesAccept client:only />

    <PortalHost client:only />
  </body>
</html>
